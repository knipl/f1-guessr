{
  "name": "@f1-guessr/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured'",
    "format": "echo 'format not configured'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
